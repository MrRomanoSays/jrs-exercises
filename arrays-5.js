// 5. Write a simple JavaScript program to join all elements of the following array into a string.

const myColor = ["Red", "Green", "White", "Black"];

const joinArrayIntoString = function (array) {
    return array.join()
}

joinArrayIntoString(myColor)
